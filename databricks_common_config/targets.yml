targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405616842293461.1.azuredatabricks.net
    variables:
      catalog: training
      schema: dev
    presets:
      artifacts_dynamic_version: true
  prod:
    mode: production
    workspace:
      host: https://adb-7405616842293461.1.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/wzounonpro@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/wzounonpro@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: training
      schema: prod
    permissions:
      - user_name: wzounonpro@gmail.com
        level: CAN_MANAGE