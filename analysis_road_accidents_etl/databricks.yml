# This is a Databricks asset bundle definition for analysis_road_accidents_etl.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: analysis_road_accidents_etl
  uuid: c8513182-bf66-413e-8333-17c88341e88a

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema_raw:
    description: The schema to use for the bronze layer
  schema_dataprep:
    description: The schema to use for the silver layer
  schema_analytics:
    description: The schema to use for the gold layer
  workspace_base_path:
    description: The base path in the workspace where resources will be deployed for the target
  

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405616842293461.1.azuredatabricks.net
    variables:
      catalog: training
      schema_raw: roaa_dev_raw
      schema_dataprep: roaa_dev_dataprep
      schema_analytics: roaa_dev_analytics
      workspace_base_path: /Workspace/Users/wzounonpro@gmail.com/.bundle/${bundle.name}/${bundle.target}
    presets:
      artifacts_dynamic_version: true
  prod:
    mode: production
    workspace:
      host: https://adb-7405616842293461.1.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/wzounonpro@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/wzounonpro@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: training
      schema_raw: roaa_prod_raw
      schema_dataprep: roaa_prod_dataprep
      schema_analytics: roaa_prod_analytics
      workspace_base_path: /Workspace/Users/wzounonpro@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: wzounonpro@gmail.com
        level: CAN_MANAGE
