# Job for geographic_reference_system ingestion.

resources:
  jobs:
    geographic_reference_system_ingestion:
      name: geographic_reference_system_ingestion

      trigger:
        pause_status: UNPAUSED
        file_arrival:
          url: ${var.volume_geographic_reference_system}

      queue:
        enabled: true

      #email_notifications:
      #  on_failure:
      #    - your_email@example.com

      parameters:
        - name: catalog
          default: ${var.catalog}
        - name: schema
          default: ${var.schema}

      tasks:
        - task_key: geographic_reference_system_ingestion_task
          description: Task to run the geographic_reference_system ingestion pipeline.
          pipeline_task:
            pipeline_id: ${resources.pipelines.geographic_reference_system_ingestion_pipeline.id}

      job_clusters:
        - job_cluster_key: job_cluster
          new_cluster:
            spark_version: 16.4.x-scala2.12
            node_type_id: Standard_D2ads_v6
            data_security_mode: SINGLE_USER
            num_workers: 0
            spark_conf:
              spark.databricks.cluster.profile: singleNode
              spark.master: local[*]
            custom_tags:
              ResourceClass: SingleNode
